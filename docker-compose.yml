version: '3.8'

services:
  vuln-scanner:
    build: .
    container_name: web-vuln-scanner
    volumes:
      - ./reports:/app/reports
    environment:
      - PYTHONUNBUFFERED=1
    entrypoint: python3 simple_main.py
    command: "--help"
